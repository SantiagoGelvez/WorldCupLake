python-dotenv==1.2.2
databricks-sql-connector==4.4.0
requests==2.34.2
# Imported directly by ingest/api_client.py for its Retry policy, so pinned here rather
# than left to float as a transitive dependency of requests.
urllib3==2.7.0
